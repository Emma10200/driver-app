# Local development placeholders for hosted submission storage
# Copy this to .env for local testing if needed.
SUBMISSION_STORAGE_BACKEND=auto
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_BUCKET=driver-applications
SUPABASE_TABLE=
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_USE_TLS=true
SMTP_USE_SSL=false
INTERNAL_NOTIFICATION_TO=
